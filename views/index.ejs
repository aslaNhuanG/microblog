<% if(!user){ %>
<div class="hero-unit">
    <h1>欢迎来到 Microblog</h1>
    <p>Microblog is base on Node.js</p>
    <p>
        <a href="/login" class="btn btn-primary btn-large">login</a>
        <a href="/reg" class="btn btn-large">register now</a>
    </p>
</div>
<% }else{ %>
    <%- partial('say')  %>
<% } %>
    <%- partial('posts',{posts:posts}) %>